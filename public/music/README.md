Add audio files to this folder and list them in `tracks.json`.

Example:

```json
[
  {
    "title": "Midnight Demo",
    "artist": "Eric Kim",
    "src": "midnight-demo.mp3"
  }
]
```

Supported browser audio formats depend on the visitor's browser. MP3 is the safest choice.
